pragma language_version 0.23;

import CompactStandardLibrary;

// The smallest possible circuit that still touches state, to answer one
// question: can ANY circuit call pass /check on this stack? If bump() works,
// the stack is fine and the problem is specific to Nightfold's circuits.

export ledger hits: Counter;

export circuit bump(): [] {
  hits.increment(1);
}
